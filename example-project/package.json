{
  "name": "model-splitter-example",
  "version": "1.0.0",
  "description": "My Wonderland project",
  "main": "js/index.ts",
  "type": "module",
  "module": "js/index.ts",
  "scripts": {
    "build": "shx cp ../lib-static/* static && esbuild --minify --bundle --sourcemap --define:WL_EDITOR=false --minify --format=esm js/index.ts --outfile=deploy/model-splitter-example-bundle.js"
  },
  "keywords": [
    "wonderland-engine"
  ],
  "devDependencies": {
    "@wonderlandengine/api": "0.9.8",
    "shx": "^0.3.4"
  },
  "dependencies": {
    "@wonderlandengine/components": "^1.0.0-rc.2"
  }
}
